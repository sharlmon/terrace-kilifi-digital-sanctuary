{
  "buildCommand": "npm run build && node scripts/fix-gh-pages.mjs",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
